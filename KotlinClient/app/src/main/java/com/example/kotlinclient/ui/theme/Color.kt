package com.example.kotlinclient.ui.theme

import androidx.compose.ui.graphics.Color

val LinearGradientStartColor = Color(0xF0FF0022) // Цвет начала градиента
val linearGradientEndColor = Color(0xFF500005) // Цвет конца градиента
val ServiceFloatingButtonColor = Color(0xFCe1333d) // Цвет

val NotificationIconColor = Color(0xFFFFD329)

val InfoIconColor = Color(0xFF02DC00)

val TemplateIconColor = Color(0xFF235BFF)











val textMain_lightTheme = Color(0xFF000000)// Цвет Основного текста
val textSecondary_lightTheme = Color(0xb04a5565) // Цвет подписи
val contrast_lightTheme = Color(0xf0e7000b) // Контрастный цвет

val background_lightTheme = Color(0xFFFFFFFF) // Первый уровень вложенности контейнеров (Header, Footer)
val mainContainerBackground_lightTheme = Color(0xFFF9FAFB) // Второй уровень вложенности контейнеров (Основной контейнер экрана)
val containerBackground_lightTheme = Color(0xFFFFFFFF) // Третий уровень вложенности контейнеров (Контейнервы внутри основонго контейнера)
val backgroundInContainer_lightTheme = Color(0xFFf3f4f6) // Четвёртый уровень вложенности контейнеров (Контейнеры внутри 3 уровня)

val divider_lightTheme = Color(0xFFe5e7eb) // Цвет Разделителей, бордеров

val textMain_darkTheme = Color(0xFFFFFFFF) // Цвет Основного текста
val textSecondary_darkTheme = Color(0xFF8b9fa9) // Цвет подписи
val contrast_darkTheme = Color(0xffe7000b) // Контрастный цвет

val background_darkTheme = Color(0xff09090b) // Первый уровень вложенности контейнеров (Header, Footer)
val mainContainerBackground_darkTheme = Color(0xff18181b) // Второй уровень вложенности контейнеров (Основной контейнер экрана)
val containerBackground_darkTheme = Color(0xFF27272a) // Третий уровень вложенности контейнеров (Контейнервы внутри основонго контейнера)
val backgroundInContainer_darkTheme = Color(0xff18181b) // Четвёртый уровень вложенности контейнеров (Контейнеры внутри 3 уровня)

val divider_darkTheme = Color(0xFF3f3f47) // Цвет Разделителей, бордеров





